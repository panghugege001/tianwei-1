<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nnti.common.dao.slave.ISlavePTProfitDao">

	<sql id="columns_sql">
		uuid as uuid,
		userId as userId,
		amount as amount,
		betCredit as betCredit,
		starttime as startTime,
		endtime as endTime,
		loginname as loginName,
		payOut as payOut,
		updatetime as updateTime
	</sql>

	<select id="queryBetCredit" parameterType="PTProfit" resultType="java.lang.Double">
		select
		betCredit
		from
		ptprofit
		where loginname = #{loginName}
		<![CDATA[and starttime >= #{startTime}]]>
	</select>

	<select id="sumBetCredit" parameterType="java.util.Map" resultType="java.lang.Double">
		select
		sum(betCredit)
		from
		ptprofit
		where loginname = #{loginName}
		<![CDATA[and starttime >= #{startTime}]]>
	</select>
</mapper>