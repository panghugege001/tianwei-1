<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nnti.common.dao.slave.ISlaveAgentVipDao">
	
	<sql id="columns_sql">
		agent as agent,
		level as level,
		activemonth as activemonth,
		activeuser as activeuser,
		registertime as registertime,
		createtime as createtime,
		remark as remark,
		historyfee as historyfee,
		type as type
	</sql>
	
    <select id="get" parameterType="java.lang.String" resultType="Const">
    	select
   		<include refid="columns_sql" />
        from
        agentvip
		WHERE id = #{0}
    </select>
    

	<select id="findAgentVipList" parameterType="java.util.Map" resultType="AgentVip">
		select
        <include refid="columns_sql" />
		from
        agentvip
		<where>
			<if test="agent != null">agent = #{agent}</if>
        </where>
          ORDER BY createtime desc
    </select>
</mapper>