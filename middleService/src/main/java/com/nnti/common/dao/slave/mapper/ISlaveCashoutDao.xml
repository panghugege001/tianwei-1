<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nnti.common.dao.slave.ISlaveCashoutDao">
	
	<sql id="columns_sql">
		pno,
		title,
		loginname,
		money,
		accountName,
		accountType,
		accountCity,
		bankAddress,
		accountNo,
		bank,
		phone,
		email,
		ip,
		remark,
		notifyNote,
		notifyPhone
	</sql>
	
    <select id="get" parameterType="java.lang.String" resultType="Cashout">
    	select
        <include refid="columns_sql"/>
        from
        cashout
        <where>
        	pno = #{0}
        </where>
    </select>
</mapper>