<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nnti.common.dao.slave.ISlaveConstDao">
	
	<sql id="columns_sql">
		id as id,
		value as value,
		shopno as shopNo
	</sql>
	
    <select id="get" parameterType="java.lang.String" resultType="Const">
    	select
   		<include refid="columns_sql" />
        from
        const
		WHERE id = #{0}
    </select>

	<select id="findConstList" parameterType="Const" resultType="Const">
		select
        <include refid="columns_sql" />
		from
        const
		<where>
			<if test="id != null">id = #{id}</if>
            <if test="value != null">and value = #{value}</if>
            <if test="shopNo != null">and shopno = #{shopNo}</if>
        </where>
    </select>
</mapper>