<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nnti.common.dao.slave.ISlaveCreditLogExtendDao">

    <sql id="columns_sql">
		id as id,
		orderId as orderId,
		createTime as createTime,
		remark as remark,
		ext1 as ext1,
		ext2 as ext2,
		ext3 as ext3,
		ext4 as ext4
	</sql>

    <select id="count" parameterType="CreditLogExtend" resultType="java.lang.Integer">
        select
        count(1)
        from
        creditlog_extend
        <where>
            <if test="orderId != null">AND orderId = #{orderId}</if>
        </where>
    </select>
</mapper>